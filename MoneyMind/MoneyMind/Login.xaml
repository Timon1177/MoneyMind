<Window x:Class="MoneyMind.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#2C3E50">

    <Grid>
        <Viewbox Stretch="Uniform">
            <Border Width="800" Height="600" Background="#ECF0F1" CornerRadius="15" Padding="30">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                    Text="MoneyMind Login"
                    FontSize="36" 
                    FontWeight="Bold"
                    Foreground="#2C3E50" 
                    HorizontalAlignment="Center"
                    Margin="0,10,0,30"/>

                    <TextBlock Grid.Row="1" 
                    Text="Benutzername" 
                    Foreground="Gray"
                    FontSize="16" 
                    Margin="5,0,0,5"/>

                    <TextBox Grid.Row="2" 
                    x:Name="txtUsername"
                    Height="40" 
                    FontSize="16" 
                    Padding="10"/>

                    <TextBlock Grid.Row="3" 
                    Text="Passwort" 
                    Foreground="Gray" 
                    FontSize="16" 
                    Margin="5,20,0,5"/>

                    <PasswordBox Grid.Row="4" 
                    x:Name="txtPassword" 
                    Height="40" 
                    FontSize="16" 
                    Padding="10"/>

                    <Button Grid.Row="5" 
                    Content="Login"
                    Height="45" Background="#2980B9"
                    Foreground="White" FontWeight="Bold"
                    FontSize="16" BorderThickness="0"
                    Margin="0,20,0,0"
                    Click="BtnLogin_Click"/>

                    <Button Grid.Row="6" 
                    Content="Registrieren"
                    Height="45" Background="#27AE60"
                    Foreground="White" FontWeight="Bold"
                    FontSize="16" BorderThickness="0"
                    Margin="0,10,0,0"
                    Click="BtnRegister_Click"/>
                </Grid>
            </Border>
        </Viewbox>
    </Grid>
</Window>
